plugins {
    id 'java-conventions'
    id 'java-library'
}

group = 'com.poke.api.middleware.application'
version = 'unspecified'

dependencies {
    implementation(project(":domain"))

    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}
